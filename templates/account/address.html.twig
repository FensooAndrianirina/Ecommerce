{% extends 'base.html.twig' %}

{% block title 'Mes adresses | Petite Boutique' %}

{% block content %}
    <a href="{{path('account_address_add')}}" class="btn btn-info float-right"> Ajaouter une addresse  </a>
    <h1>Mes adresses</h1>
    C'est dans cet espace que vous allez pouvoir gérer vos adresses.<br>
    <a href="{{path('account')}}" class="mt-4"> Retour  </a><hr>

    {% if app.user.addresses|length == 0 %}
        <p class="text-center">
            Vous n'avez pas encore d'adresse dans votre compte client. 
            Pour en ajouter une, veuillez <a href="{{path('account_address_add')}}"> cliquer ici </a> 
        <p>
    {% else %}
        <div class="row pb-3">
            {% for address in app.user.addresses %}
                <div class="col-md-4 mt-2">
                    <div class="card" >
                        <div class="card-body">
                            <h5 class="card-title">{{address.name}}</h5>
                            <p class="card-text">
                                {{address.address}}, <br>
                                {{address.postal}} - {{address.city}} - {{address.country}}
                            </p>
                            <a href="{{ path('account_address_edit', {'id' : address.id}) }}">Modifier</a> | 
                            <a href="{{ path('account_address_delete', {'id' : address.id}) }}" class="">Supprimer</a>
                        </div>
                    </div>    
                </div>
            {% endfor %}
        </div>       
    {% endif %}
{% endblock %}